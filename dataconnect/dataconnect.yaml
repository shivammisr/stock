specVersion: "v1beta"
serviceId: "stock"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "stock-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
